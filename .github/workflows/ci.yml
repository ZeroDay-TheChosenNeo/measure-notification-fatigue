name: task-ci
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/run_task.sh task oracle
      - run: ./scripts/run_task.sh task nop
